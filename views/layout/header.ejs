<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="<%= assetFingerprint('/stylesheets/style.css')%>"
    />
  </head>
  <body>
    <header class="header">
      <div class="container">
        <a href="/"><h1 class="logo">Blog</h1></a>
        <ul class="menu">
          <li>
              <a class="link" href="/user/<%= user.login %>"><%= user.login %></a>
          </li>
          <li>
              <% if(user.login && user.id){ %>
                <a href="/api/auth/logout" class="exit">Вийти</a>
              <% }else{ %>
                <a href="/api/auth/logout" class="exit">Вхід</a>
              <% } %>
          </li>
        </ul>
      </div>
    </header>
  </body>
</html>
